# Jekyll configuration for the Bloom Rise legal pages site.
# GitHub Pages uses this to set the site title (shown in the browser tab and
# the page header) instead of defaulting to the repository name.
title: Bloom Rise
description: Bloom Rise — legal pages (Privacy Policy, account deletion, support)
